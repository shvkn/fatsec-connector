{
  "name": "fatsecret-account-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "description": "A small web application for connecting and managing FatSecret accounts.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "node --test test/*.test.js",
    "test:ydb": "node test/ydb.integration.js",
    "check": "node --check src/server.js && node --check src/config.js && node --check src/database.js && node --check src/fatsecret.js && node --check src/security.js && node --check public/app.js"
  },
  "dependencies": {
    "@ydbjs/auth": "^6.3.1",
    "@ydbjs/core": "^6.3.1",
    "@ydbjs/query": "^6.3.0",
    "@ydbjs/value": "^6.0.8",
    "express": "^5.1.0"
  }
}
